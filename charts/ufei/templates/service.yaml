apiVersion: v1
kind: Service
metadata:
  name: {{ include "ufei.name" . }}
  labels:
    {{- include "ufei.selector" . | nindent 4 }}
spec:
  selector:
    {{- include "ufei.selector" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
